import { MemoryCardPayload } from '../types';
import { StorageBackend } from './backend';
import { StorageBackendOptions } from './backend-config';
export declare class StorageNop extends StorageBackend {
    constructor(name: string, options: StorageBackendOptions);
    toString(): string;
    load(): Promise<MemoryCardPayload>;
    save(_: MemoryCardPayload): Promise<void>;
    destroy(): Promise<void>;
}
//# sourceMappingURL=nop.d.ts.map
import { MemoryCardPayload } from '../types';
import { StorageBackendOptions } from './backend-config';
export declare abstract class StorageBackend {
    protected name: string;
    protected options: StorageBackendOptions;
    constructor(name: string, options: StorageBackendOptions);
    abstract save(payload: MemoryCardPayload): Promise<void>;
    abstract load(): Promise<MemoryCardPayload>;
    abstract destroy(): Promise<void>;
}
//# sourceMappingURL=backend.d.ts.map
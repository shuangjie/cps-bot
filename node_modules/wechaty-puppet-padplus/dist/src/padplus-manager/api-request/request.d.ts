/// <reference types="node" />
import { GrpcGateway } from '../../server-manager/grpc-gateway';
import { ApiType } from '../../server-manager/proto-ts/PadPlusServer_pb';
import { GrpcEventEmitter } from '../../server-manager/grpc-event-emitter';
export interface RequestOption {
    data?: any;
    apiType: ApiType;
}
export declare class RequestClient {
    private grpcGateway;
    private emitter;
    private dedupeApi;
    constructor(grpcGateway: GrpcGateway, emitter: GrpcEventEmitter);
    request(option: RequestOption): Promise<any>;
    uploadFile(filename: string, stream: NodeJS.ReadableStream): Promise<string>;
}
//# sourceMappingURL=request.d.ts.map